'use client'

import { P } from "@/app/components/ui/paragraph";
import { Box } from "@/app/components/ui/layout/box";
import { FC } from "react";

const InspectTool: FC = () => {
  return (
    <Box>
      <P>This is the inspect tool.</P>
    </Box>
  );
}

export default InspectTool;
